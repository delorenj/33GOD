id: pipeline-mcp-hub
name: Pipeline MCP Hub
role: tool-dispatch-gateway
repo: ../mcp-hub
description: Small MCP surface that gates large domain toolsets behind discovery and dispatch.
compose:
  files: []
  profiles: [integrations]
health:
  commands:
    - cd ../mcp-hub && python -m mcp_hub --help
source_of_truth:
  - ../mcp-hub/mcp_hub
  - ../mcp-hub/README.md
changelog:
  topics: [mcp, tools, plane, domains]
public_card:
  title: Tool Gateway
  promise: Compact MCP access to Plane, Bloodbank, lifecycle, and future domains.
backfills: []
