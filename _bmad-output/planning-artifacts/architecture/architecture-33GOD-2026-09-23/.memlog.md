---
scope: DeloHQ executive experience on Holocene /hq
purpose: build-substrate
altitude: feature
updated: 2026-09-23T01:28
---

- (direction) Use Coaching path for a focused DeloHQ feature-altitude architecture spine; produce the spine only and defer additional diagrams or solution-design prose.
- (constraint) The finalized DeloHQ PRD is binding for this run: DeloHQ is an executive experience, not a system of record; binds FR-1 through FR-16 and UJ-1 through UJ-4.
- (constraint) Current brownfield deployment is binding: Holocene web serves /hq in the holocene-web Compose service, holocene-api.service owns the host API, and the root platform Compose file owns the live web deployment.
- (constraint) Current authority boundary is binding: Flume owns workforce authority; PJangler owns project identity; Krebs/Pilot own ticket lifecycle; Bloodbank/Candystore own event transport and history; Hermes owns runtime execution.
- (constraint) Current /hq reality is binding: the browser polls an authenticated Next.js proxy; Holocene API reads registry, org overlay, runtime state, and Candystore history; the web must not read those sources directly.
- (decision) Adopt a projection-oriented anti-corruption layer with an explicit command gateway as the DeloHQ paradigm; this prevents independently built surfaces from inventing domain ownership or command semantics.
- (decision) Keep DeloHQ inside Holocene for v1 with no DeloHQ-owned database, event stream, workforce registry, or separate service; this prevents a second executive control plane from emerging.
- (decision) Make every DeloHQ read a Holocene API view model with source, timestamps, freshness state, evidence references, and schema version; this prevents the Company, Now, Inbox, and Office surfaces from making incompatible truth claims.
- (decision) Use opaque canonical identifiers and preserve owning-system references for Employees, Projects, tickets, events, Actions, and receipts; display names are labels only and never identity.
- (decision) Route all consequential DeloHQ operations through a named, allowlisted command gateway with correlation and idempotency; the canonical system owns state transitions and DeloHQ never treats accepted as completed.
- (decision) Keep Telegram Mini App initData verification at the public /hq boundary and derive the actor context there; the internal API receives only named proxied operations and never becomes the browser's direct authority path.
- (decision) Preserve the split deployment and degrade to explicit stale or Unknown states when any source or projection cannot support a stronger claim; absence or an empty result is never healthy by inference.
- (question) Select numeric freshness budgets per Company, Now, Inbox, and Agent Office projection before projection implementation; Architecture and UX own the decision.
- (question) Select the first bounded Action catalog and canonical receipt providers beyond approve, reject, and acknowledge before story acceptance criteria are written.
- (question) Choose the Flume-to-Holocene projection transport and migration path from the current org.yaml presentation overlay before FR-1 implementation.
- (question) Define the retained and retired /hq capabilities, notification classes, and Now/Inbox grouping policy during the UX and architecture handoff.
- (question) Define the bounded Company Memory fields and evidence policy before Agent Office exposes Hindsight context.
- (event) Reconciled the finalized PRD, current Holocene /hq implementation and deployment, and Flume/platform authority contracts; current org, action, and envelope paths remain transitional until the deferred contracts are implemented.
- (decision) Tighten the spine with one holocene/packages/delohq-contracts registry: shared view models, freshness and attention vocabularies, item identity, command envelopes, receipt normalization, actor context, and contract tests prevent parallel units from diverging.
- (change) Resolve reviewer findings by requiring one workforce projection snapshot, shared Now/Inbox grouping and attention policy, canonical receipt-state mapping, fail-closed actor fields, and a platform toolchain convergence handoff.
- (event) spine finalized
